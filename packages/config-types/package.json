{
  "name": "config-types",
  "private": true,
  "type": "module",
  "version": "1.38.12",
  "description": "Core type definitions for Ottehr configuration contracts",
  "main": "index.ts",
  "types": "index.ts",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "oxlint --type-aware --report-unused-disable-directives --deny-warnings .",
    "lint:fix": "oxlint --type-aware --report-unused-disable-directives --deny-warnings --fix ."
  }
}
