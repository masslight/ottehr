{
  "name": "pacs-poll",
  "version": "1.38.1",
  "private": true,
  "scripts": {
    "start": "NODE_OPTIONS='--preserve-symlinks' tsx src/index.ts",
    "build": "tsc",
    "bundle": "NODE_OPTIONS='--preserve-symlinks' tsx bundle.ts",
    "lint": "NODE_OPTIONS='--max-old-space-size=8192' eslint --ext .ts,.js --report-unused-disable-directives --max-warnings 0 .",
    "lint:fix": "eslint --ext .ts,.js --report-unused-disable-directives --max-warnings 0 --fix ."
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@sentry/esbuild-plugin": "^3.5.0",
    "esbuild": "^0.25.1",
    "esbuild-plugin-copy": "^2.1.1"
  }
}
